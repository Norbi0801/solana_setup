<!-- Sekcja "Wykonaj program" -->
# Wykonaj program

- W katalogu `client` uruchom `npm install`, a następnie `npm run init`, aby wysłać początkową instrukcję.
- Użyj `npm run update -- "Nowa wiadomość"`, aby symulować drugi krok przewodnika.
- Zweryfikuj logi programu w `solana logs <signature>`.
