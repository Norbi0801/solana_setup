<!-- Kroki do sekcji "Składanie zapytania RPC" -->
# Składanie zapytania RPC

1. `npm install` w katalogu `client`.
2. `npm run rpc` – pobiera ostatni blok i wypisuje szczegóły ("Pokaż wynik").
3. `SOLANA_RPC_URL=http://127.0.0.1:8899 npm run custom -- <adres>` – demonstruje zmianę celu RPC.
