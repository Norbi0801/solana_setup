<!-- Notatki do sekcji "Użyj klienta" -->
# Użyj klienta

- `custom_query.ts` przyjmuje adres i wypisuje podstawowe informacje o koncie.
- Dodając drugi argument `-- json` możesz pipować wynik do narzędzi CLI, np. `| jq`.
- Integruj skrypt z `npm run custom -- <adres>` podczas debugowania sieci.
