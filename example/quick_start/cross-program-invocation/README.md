<!-- Rozdział "Cross Program Invocation" -->
# Cross Program Invocation

Ten katalog demonstruje dwa programy Anchor: `callee`, który aktualizuje konto, oraz `caller`, który wywołuje CPI do `callee`. Dodatkowe skrypty TypeScript pokazują przygotowanie kont i uruchomienie instrukcji.
