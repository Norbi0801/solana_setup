<!-- Podrozdział "Dodaj własne instrukcje serializatora" -->
# Dodaj własne instrukcje serializatora

- Plik `client/serializer.ts` pokazuje ręczną serializację Borsh dla struktur `BuilderData`.
- CLI (`client/cli.ts`) wypisuje zakodowane dane, aby łatwo porównać je z IDL.
