<!-- Podrozdział "Dodaj użycie TypeScript" -->
# Dodaj użycie TypeScript

- Katalog `client` zawiera skrypty `create.ts` i `update.ts`, które wykorzystują Anchor Provider.
- `tsconfig.json` ustawia moduł `commonjs`, aby działać z `ts-node` bez dodatkowej konfiguracji.
