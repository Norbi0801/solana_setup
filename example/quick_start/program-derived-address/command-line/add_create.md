<!-- Podrozdział "Dodaj Create" -->
# Dodaj Create

- Komenda `create` w CLI wywołuje `program.methods.create` z obliczonym PDA i przekazanym `BuilderData`.
- Loguje zakodowane dane, co pomaga debugować w zgodzie z dokumentacją.
