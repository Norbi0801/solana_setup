<!-- Sekcja "Wprowadź wiersz poleceń" -->
# Wprowadź wiersz poleceń

- `client/cli.ts` wykorzystuje `yargs`, by zapewnić komendę `create` i `update` wraz z flagami.
- Skrypt możesz uruchomić przez `yarn ts-node cli.ts create --program-id <ID> --mode 1 --name Demo`.
